<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuario</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Editar Usuario</h1>
    <div class="container">
        <form action="/edit/<%= user.id %>" method="POST">
            <input type="text" name="nombres" placeholder="Nombres" value="<%= user.nombres %>" required>
            <br>
            <input type="text" name="apellidos" placeholder="Apellidos" value="<%= user.apellidos %>" required>
            <br>
            <input type="text" name="numero_cuenta" placeholder="Número de Cuenta" value="<%= user.numero_cuenta %>" required>
            <br>
            <input type="number" name="edad" placeholder="Edad" value="<%= user.edad %>" required>
            <br>
            <select name="genero" required>
                <option value="" disabled>Género</option>
                <option value="Masculino" <%= user.genero === "Masculino" ? "selected" : "" %>>Masculino</option>
                <option value="Femenino" <%= user.genero === "Femenino" ? "selected" : "" %>>Femenino</option>
                <option value="Otro" <%= user.genero === "Otro" ? "selected" : "" %>>Otro</option>
            </select>
            <br>
            <input type="text" name="carrera" placeholder="Carrera" value="<%= user.carrera %>" required>
            <br>
            <input type="text" name="telefono" placeholder="Teléfono" value="<%= user.telefono %>" required>
            <br>
            <button type="submit">Actualizar</button>
            <a href="/">Cancelar</a>
        </form>
    </div>
</body>
</html>
